# @tsonic/csharp-core

C# implementation package for target-specific `@tsonic/core` runtime support.

The portable `@tsonic/core` package owns source-level marker APIs such as
numeric aliases, attributes, `field<T>`, and `out/ref/inref` markers. This
package owns the C# target implementation artifacts that reference .NET runtime
identity.
